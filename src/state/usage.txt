// USAGE SNIPPET (inside any client page/component with swipe logic)
//
// import { useSwipeStore } from '../state/swipeStore';
//
// const routes = useSwipeStore((s) => s.routes);
// const index = useSwipeStore((s) => s.index);
// const setIndex = useSwipeStore((s) => s.setIndex);
// const nextIdx = useSwipeStore.getState().nextIndex();
// const prevIdx = useSwipeStore.getState().prevIndex();
//
// - Use `routes` and `index` to render pager/active state.
// - When swipe threshold is met, animate off-screen then:
//     - call history/router to navigate to `routes[target]`
//     - call `setIndex(target)` to keep the store in sync.